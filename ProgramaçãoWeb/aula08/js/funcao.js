/* Funções

Uma função JavaScript é um bloco de código projetado para executar uma tarefa específica.

É como uma pequena "fábrica" onde você faz uma entrada e ele te dá uma saída.

Pode ser encarado como "mini-programas" projetados para fazer uma tarefa que vai contribuir para todo o código.

Uma função JavaScript é executada quando "algo" a invoca(chama-a).

*/

/*function ação(parâmetro) {
    return (conteúdo que irá retornar)
}*/

/*function calculo(n1, n2) {
    return n1 / n2;
}

var tcalculo = calculo(10, 2)
alert(tcalculo)*/

/*function conversao(real, cotacaoDolar) {
    return real*cotacaoDolar;
}

var valorReal = 7.89;
var cotacao = 5.10;

var total = conversao(valorReal, cotacao);

alert(`O valor em real é R$${valorReal} e o valor em dólar é U$${total}`);*/

function alertaHello() {
    alert("Hello")
}
